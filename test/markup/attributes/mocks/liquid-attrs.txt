<div
  id="{{ first }}">
  <div
    id="ssss"
    {{ second.attr }}
    class="xxxxxx {{- within.value -}}"
    data-x="zzzz"
    {{ last.attr }}>

    <div
      data-attr="{{ within.value }}"
      class={{ no.quotations }}
      id="{% if x == xx %} {{ condition.in_value -}} {% endif -%}">

      <div
        id={{ no.quotations }}
        class="xxx"
        {% unless x %}
        data-{{ attr_between | filter: 'xxx' }}-xxx="ssss"
        {% endunless %}></div>
    </div>
  </div>
</div>
