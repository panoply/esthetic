# Snapshot report for `tests/cases/attributes.test.mjs`

The actual snapshot is saved in `attributes.test.mjs.snap`.

Generated by [AVA](https://avajs.dev).

## Delimiter handling

> ### Snapshot 1
> Testing HTML tag delimiter characters `>` and `<` within HTML attribute values.

    `<div␊
      data-a="a > b"␊
      data-b="c < d"␊
      data-c="e f > g < h"␊
      data-d="j < k > l"></div>`

> ### Snapshot 2
> Testing Liquid token containing HTML tag delimiter characters `>` and `<` within attribute values

    `<div␊
      data-a="{% if a > b %}test-1{% endif %}"␊
      data-b="{% if c < d %}test-2{% endif %}"␊
      data-c="{% unless e > f and g < h %}test-3{% elsif i > j %}test-4{% endunless %}"␊
      data-f="{% if  < k and l > m %}test-3{% endif %}"></div>`

> ### Snapshot 3
> Testing Liquid tokens as attributes and containing HTML tag delimiter characters `>` and `<`

    `<div␊
      {% if a > b %}␊
      data-a␊
      {% endif %}␊
      {% if c < d %}␊
      data-b␊
      {% endif %}␊
      {% unless e > f and g < h %}␊
      data-c="a > b"{% elsif i > j %}␊
      data-d="a > b"{% endunless %}␊
      {% if < k and l > m %}␊
      {{ output_1 | filter: '>' | filter: '<' }}␊
      {% else %}␊
      {{ output_2 | filter: '<' | filter: '>' }}␊
      {% endif %}></div>`

## Quote handling

> ### Snapshot 1
> Testing single quotation `'` characters nesting of Liquid tokens contained within HTML attribute values.

    `{% for step in steps %}␊
      <div␊
        class="{% if 'xx' %} {{ x | filter: 'quotes' }}{% endif %}"></div>␊
    {% endfor %}`

> ### Snapshot 2
> Testing double quotation `"` characters nesting of Liquid tokens contained within HTML attribute values.

    `{% for step in steps %}␊
      <div␊
        class="{% if "xx" %} {{ x | filter: "quotes" }}{% endif %}"></div>␊
    {% endfor %}`

## Preserve attribute structures

> ### Snapshot 1
> Testing Liquid attribute structures. The `id="foo"` attribute will be moved inline. The Liquid statement will be preserved and left intact as no space separators are passed.

    `<div␊
      id="foo"␊
      {% if foo %}␊
      data-{% if x %}-foo{% else %}-bar{% endif %}="{{ x }}"␊
      {% endif %}></div>`

> ### Snapshot 2
> Testing Liquid attribute structures which will conditionally output attributes. The expressions will remain intact, with the `id=foo` being inlined. The `{% if x %}`, `{% elsif %}` will be preserved as per expression.

    `<div␊
      id="foo"␊
      {% if x %}data-attr="x"␊
      {% elsif %}␊
      data-id="x"{% endif %}></div>`

> ### Snapshot 3
> Testing single chained structure which will result in either a boolean attribute type or attribute value.

    `<div␊
      data-{% if x %}id="foo"{% else %}name{% endif %}></div>`

> ### Snapshot 4
> Testing Liquid attribute structures wherein the chain contains output Liquid tokens as boolean type attributes.
> The output tokens are conditionally structured. All expression will remain intact.

    `<div␊
      id="foo"␊
      data-bar␊
      {{ output.attr | filter: '' }}␊
      data-id="foo"␊
      {% if x %}␊
      {{ attr.2 }}␊
      {% endif %}␊
      {% if x %}data-x={{ foo }}{% else %}{{ attr.3 }}{% endif %}></div>`

> ### Snapshot 5
> Testing conditional attribute structures with unquoted Liquid value and attribute containing liquid
> conditionals with both unquoted and quoted liquid values.

    `<div␊
      data{% if x %}-foo{% else %}-bar{% endif %}={{ x }}␊
      {% tag %}={% if x %}"foo"{% else %}{{ bar }}{% endif %}></div>`

> ### Snapshot 6
> Testing various attribute structures both Liquid and HTML types. Double quoted, Single quoted and no quoted attribute
> expression. This is an extreme edge case.

    `<div␊
      named␊
      class="x"␊
      data-attr␊
      data-nq=x␊
      {{ attribute }}␊
      {{ attribute | filter: '' }}␊
      id={{ unquoted.value }}␊
      data-dq="{{ dq.value }}"␊
      data-sq='{{ sq.value }}'␊
      {{ attr }}="liquid-output-attr"␊
      {% tag %}="liquid-tag-attr"␊
      {% if x %}data-if{% elsif x > 0 %}data-elsif{% else %}data-else{% endif %}={{ value}}␊
      data-{% if x %}id="foo"{% else %}name{% endif %}></div>`

> ### Snapshot 7
> Testing nested conditional Liquid expressions with infused `style=""` attribute that is using liquid output tokens
> as values.

    `<div␊
      id={{ x }}>␊
    ␊
      <div␊
        id="x"␊
        class="xxx xxx xxx"␊
        {% if x == 100 %}␊
        data-{% if x %}{{ x }}{%- else -%}foo{%- endif %}-id="xxx"␊
        {% elsif x != 200 %}␊
        {% unless u == 'foo' and x == 'bar' %}␊
        data-{{ object.prop | filter }}␊
        {% endunless %}␊
        {% endif %}␊
        aria-x="foo"␊
        style="background-color: {{ bg.color }}; font-size: 20px;"></div>␊
    ␊
    ␊
    </div>`

## Force attributes

> ### Snapshot 1
> Forcing Liquid attribute control structures that are conditionally chaining. Ensures that
> the structure of `data-` is left intact, whereas the starting `{% if foo %}` and ending `{% endif %}`
> will be forced onto new lines.

    `<div␊
      id="foo"␊
      {% if foo %}␊
      data-{% if x %}-foo{% else %}-bar{% endif %}="{{ x }}"␊
      {% endif %}␊
      class="bar"></div>`

> ### Snapshot 2
> Tests conditional expressed Liquid attribute structures with space separators. Each space separated expression
> will be forced on to new lines.

    `<div␊
      id="foo"␊
      {% if x %}␊
      data-attr="x"{% elsif %}␊
      data-id="x"{% endif %}></div>`

> ### Snapshot 3
> Testing chained conditional attribute structure. The `data-{% if x %}` will either be a boolean attribute
> or value attribute based on the condition. The entire structure will be force onto new line.

    `<div␊
      data-{% if x %}id="foo"{% else %}name{% endif %}></div>`

> ### Snapshot 4
> Testing Liquid output tag and control tag infusion. All attributes will be forced onto new lines but
> the `{% if x %}` structure will be preserved and remain intact.

    `<div␊
      id="foo"␊
      data-bar␊
      {{ output.attr | filter: '' }}␊
      id="foo"␊
      {% if x %}{{ attr.2 }}{% endif %}></div>`

> ### Snapshot 5
> Testing nested attribute forcing. This case is rather common and intends to affirm the correct output structures
> typical in most projects but also an exteme edge case scenario.

    `<div␊
      id="foo"␊
      data-bar␊
      class="xxxxx"␊
      {{ output.attr | filter: '' }}>␊
      <ul>␊
        <li␊
          {% if x %}{{ attr.2 }}{% endif %}>Hello</li>␊
        <li␊
          data-attr-1="one"␊
          data-attr-2="two"␊
          data-attr-3="three">␊
          <a␊
            href="/some-url"␊
            {% unless xx %}␊
            data-boolean␊
            {% else %}␊
            id="some-id"{% endunless %}>␊
            Click me!␊
          </a>␊
        </li>␊
        <li␊
          data-attr-one="1"␊
          data-attr-two="2"␊
          data-attr-three="3"␊
          {% for x in arr %}␊
          data-{{ x }}␊
          {% endfor %}>␊
          <div␊
            id="{{ id | filter: 'foo' }}"␊
            data-{% if x %}id="foo"{% else %}name{% endif %}>␊
            <ul␊
              class="xxxxxx"␊
              aria-attr="xxxxxxxx"␊
              {{ output.attr | filter: '' }}>␊
              <li␊
                id="x"␊
                class="xxx xxx xxx"␊
                data-{% if x %}{{ x }}{%- else -%}foo{%- endif %}␊
                -id="xxx"␊
                aria-x="foo">␊
                Hello World␊
              </li>␊
            </ul>␊
          </div>␊
        </li>␊
      </ul>␊
    </div>`

## Wrap based attribute forcing

> ### Snapshot 1
> Forcing Liquid attribute control structures that are conditionally chaining. Ensures that
> the structure of `data-` is left intact, whereas the starting `{% if foo %}` and ending `{% endif %}`
> will be forced onto new lines.

    `<div id="foo"␊
      {% if foo %}␊
      data-{% if x %}-foo{% else %}-bar{% endif %}="{{ x }}"␊
      {% endif %}␊
      class="bar"></div>`

> ### Snapshot 1
> Forcing Liquid attribute control structures that are conditionally chaining. Ensures that
> the structure of `data-` is left intact, whereas the starting `{% if foo %}` and ending `{% endif %}`
> will be forced onto new lines.

    `<div id="foo"␊
      {% if foo %}␊
      data-{% if x %}-foo{% else %}-bar{% endif %}="{{ x }}"␊
      {% endif %}␊
      class="bar"></div>`

> ### Snapshot 1
> Forcing Liquid attribute control structures that are conditionally chaining. Ensures that
> the structure of `data-` is left intact, whereas the starting `{% if foo %}` and ending `{% endif %}`
> will be forced onto new lines.

    `<div id="foo"␊
      {% if foo %}␊
      data-{% if x %}-foo{% else %}-bar{% endif %}="{{ x }}"␊
      {% endif %}␊
      class="bar"></div>`

> ### Snapshot 1
> Forcing Liquid attribute control structures that are conditionally chaining. Ensures that
> the structure of `data-` is left intact, whereas the starting `{% if foo %}` and ending `{% endif %}`
> will be forced onto new lines.

    `<div id="foo"␊
      {% if foo %}␊
      data-{% if x %}-foo{% else %}-bar{% endif %}="{{ x }}"␊
      {% endif %}␊
      class="bar"></div>`

> ### Snapshot 1
> Forcing Liquid attribute control structures that are conditionally chaining. Ensures that
> the structure of `data-` is left intact, whereas the starting `{% if foo %}` and ending `{% endif %}`
> will be forced onto new lines.

    `<div id="foo"␊
      {% if foo %} data-{% if x %}-foo{% else %}-bar{% endif %}="{{ x }}" {% endif %} class="bar"></div>`

> ### Snapshot 1
> Forcing Liquid attribute control structures that are conditionally chaining. Ensures that
> the structure of `data-` is left intact, whereas the starting `{% if foo %}` and ending `{% endif %}`
> will be forced onto new lines.

    `<div id="foo"␊
      {% if foo %} data-{% if x %}-foo{% else %}-bar{% endif %}="{{ x }}" {% endif %} class="bar"></div>`

> ### Snapshot 2
> Tests conditional expressed Liquid attribute structures with space separators. Each space separated expression
> will be forced on to new lines.

    `<div id="foo"␊
      {% if x %}␊
      data-attr="x"{% elsif %}␊
      data-id="x"{% endif %}></div>`

> ### Snapshot 2
> Tests conditional expressed Liquid attribute structures with space separators. Each space separated expression
> will be forced on to new lines.

    `<div id="foo"␊
      {% if x %}␊
      data-attr="x"{% elsif %}␊
      data-id="x"{% endif %}></div>`

> ### Snapshot 2
> Tests conditional expressed Liquid attribute structures with space separators. Each space separated expression
> will be forced on to new lines.

    `<div id="foo"␊
      {% if x %}␊
      data-attr="x"{% elsif %}␊
      data-id="x"{% endif %}></div>`

> ### Snapshot 2
> Tests conditional expressed Liquid attribute structures with space separators. Each space separated expression
> will be forced on to new lines.

    `<div id="foo"␊
      {% if x %} data-attr="x"{% elsif %} data-id="x"{% endif %}></div>`

> ### Snapshot 2
> Tests conditional expressed Liquid attribute structures with space separators. Each space separated expression
> will be forced on to new lines.

    `<div id="foo"␊
      {% if x %} data-attr="x"{% elsif %} data-id="x"{% endif %}></div>`

> ### Snapshot 2
> Tests conditional expressed Liquid attribute structures with space separators. Each space separated expression
> will be forced on to new lines.

    `<div id="foo"␊
      {% if x %} data-attr="x"{% elsif %} data-id="x"{% endif %}></div>`

> ### Snapshot 3
> Testing chained conditional attribute structure. The `data-{% if x %}` will either be a boolean attribute
> or value attribute based on the condition. The entire structure will be force onto new line.

    '<div data-{% if x %}id="foo"{% else %}name{% endif %}></div>'

> ### Snapshot 3
> Testing chained conditional attribute structure. The `data-{% if x %}` will either be a boolean attribute
> or value attribute based on the condition. The entire structure will be force onto new line.

    '<div data-{% if x %}id="foo"{% else %}name{% endif %}></div>'

> ### Snapshot 3
> Testing chained conditional attribute structure. The `data-{% if x %}` will either be a boolean attribute
> or value attribute based on the condition. The entire structure will be force onto new line.

    '<div data-{% if x %}id="foo"{% else %}name{% endif %}></div>'

> ### Snapshot 3
> Testing chained conditional attribute structure. The `data-{% if x %}` will either be a boolean attribute
> or value attribute based on the condition. The entire structure will be force onto new line.

    '<div data-{% if x %}id="foo"{% else %}name{% endif %}></div>'

> ### Snapshot 3
> Testing chained conditional attribute structure. The `data-{% if x %}` will either be a boolean attribute
> or value attribute based on the condition. The entire structure will be force onto new line.

    '<div data-{% if x %}id="foo"{% else %}name{% endif %}></div>'

> ### Snapshot 3
> Testing chained conditional attribute structure. The `data-{% if x %}` will either be a boolean attribute
> or value attribute based on the condition. The entire structure will be force onto new line.

    '<div data-{% if x %}id="foo"{% else %}name{% endif %}></div>'

> ### Snapshot 4
> Testing Liquid output tag and control tag infusion. All attributes will be forced onto new lines but
> the `{% if x %}` structure will be preserved and remain intact.

    `<div id="foo"␊
      data-bar␊
      {{ output.attr | filter: '' }}␊
      id="foo"␊
      {% if x %}{{ attr.2 }}{% endif %}></div>`

> ### Snapshot 4
> Testing Liquid output tag and control tag infusion. All attributes will be forced onto new lines but
> the `{% if x %}` structure will be preserved and remain intact.

    `<div id="foo"␊
      data-bar␊
      {{ output.attr | filter: '' }}␊
      id="foo"␊
      {% if x %}{{ attr.2 }}{% endif %}></div>`

> ### Snapshot 4
> Testing Liquid output tag and control tag infusion. All attributes will be forced onto new lines but
> the `{% if x %}` structure will be preserved and remain intact.

    '<div id="foo" data-bar {{ output.attr | filter: \'\' }} id="foo" {% if x %}{{ attr.2 }}{% endif %}></div>'

> ### Snapshot 4
> Testing Liquid output tag and control tag infusion. All attributes will be forced onto new lines but
> the `{% if x %}` structure will be preserved and remain intact.

    '<div id="foo" data-bar {{ output.attr | filter: \'\' }} id="foo" {% if x %}{{ attr.2 }}{% endif %}></div>'

> ### Snapshot 4
> Testing Liquid output tag and control tag infusion. All attributes will be forced onto new lines but
> the `{% if x %}` structure will be preserved and remain intact.

    '<div id="foo" data-bar {{ output.attr | filter: \'\' }} id="foo" {% if x %}{{ attr.2 }}{% endif %}></div>'

> ### Snapshot 4
> Testing Liquid output tag and control tag infusion. All attributes will be forced onto new lines but
> the `{% if x %}` structure will be preserved and remain intact.

    '<div id="foo" data-bar {{ output.attr | filter: \'\' }} id="foo" {% if x %}{{ attr.2 }}{% endif %}></div>'

> ### Snapshot 5
> Testing nested attribute forcing. This case is rather common and intends to affirm the correct output structures
> typical in most projects but also an exteme edge case scenario.

    `<div id="foo"␊
      data-bar␊
      class="xxxxx"␊
      {{ output.attr | filter: '' }}>␊
      <ul>␊
        <li {% if x %}{{ attr.2 }}{% endif %}>Hello</li>␊
        <li data-attr-1="one"␊
          data-attr-2="two"␊
          data-attr-3="three">␊
          <a href="/some-url"␊
            {% unless xx %}␊
            data-boolean␊
            {% else %}␊
            id="some-id"{% endunless %}>␊
            Click me!␊
          </a>␊
        </li>␊
        <li data-attr-one="1"␊
          data-attr-two="2"␊
          data-attr-three="3"␊
          {% for x in arr %}␊
          data-{{ x }}␊
          {% endfor %}>␊
          <div id="{{ id | filter: 'foo' }}"␊
            data-{% if x %}id="foo"{% else %}name{% endif %}>␊
            <ul class="xxxxxx"␊
              aria-attr="xxxxxxxx"␊
              {{ output.attr | filter: '' }}>␊
              <li id="x"␊
                class="xxx xxx xxx"␊
                data-{% if x %}{{ x }}{%- else -%}foo{%- endif %}␊
                -id="xxx"␊
                aria-x="foo">␊
                Hello World␊
              </li>␊
            </ul>␊
          </div>␊
        </li>␊
      </ul>␊
    </div>`

> ### Snapshot 5
> Testing nested attribute forcing. This case is rather common and intends to affirm the correct output structures
> typical in most projects but also an exteme edge case scenario.

    `<div id="foo"␊
      data-bar␊
      class="xxxxx"␊
      {{ output.attr | filter: '' }}>␊
      <ul>␊
        <li {% if x %}{{ attr.2 }}{% endif %}>Hello</li>␊
        <li data-attr-1="one"␊
          data-attr-2="two"␊
          data-attr-3="three">␊
          <a href="/some-url"␊
            {% unless xx %}␊
            data-boolean␊
            {% else %}␊
            id="some-id"{% endunless %}>␊
            Click me!␊
          </a>␊
        </li>␊
        <li data-attr-one="1"␊
          data-attr-two="2"␊
          data-attr-three="3"␊
          {% for x in arr %}␊
          data-{{ x }}␊
          {% endfor %}>␊
          <div id="{{ id | filter: 'foo' }}"␊
            data-{% if x %}id="foo"{% else %}name{% endif %}>␊
            <ul class="xxxxxx"␊
              aria-attr="xxxxxxxx"␊
              {{ output.attr | filter: '' }}>␊
              <li id="x"␊
                class="xxx xxx xxx"␊
                data-{% if x %}{{ x }}{%- else -%}foo{%- endif %}␊
                -id="xxx"␊
                aria-x="foo">␊
                Hello World␊
              </li>␊
            </ul>␊
          </div>␊
        </li>␊
      </ul>␊
    </div>`

> ### Snapshot 5
> Testing nested attribute forcing. This case is rather common and intends to affirm the correct output structures
> typical in most projects but also an exteme edge case scenario.

    `<div id="foo" data-bar class="xxxxx" {{ output.attr | filter: '' }}>␊
      <ul>␊
        <li {% if x %}{{ attr.2 }}{% endif %}>Hello</li>␊
        <li data-attr-1="one" data-attr-2="two" data-attr-3="three">␊
          <a href="/some-url"␊
            {% unless xx %}␊
            data-boolean␊
            {% else %}␊
            id="some-id"{% endunless %}>␊
            Click me!␊
          </a>␊
        </li>␊
        <li data-attr-one="1"␊
          data-attr-two="2"␊
          data-attr-three="3"␊
          {% for x in arr %}␊
          data-{{ x }}␊
          {% endfor %}>␊
          <div id="{{ id | filter: 'foo' }}"␊
            data-{% if x %}id="foo"{% else %}name{% endif %}>␊
            <ul class="xxxxxx" aria-attr="xxxxxxxx" {{ output.attr | filter: '' }}>␊
              <li id="x"␊
                class="xxx xxx xxx"␊
                data-{% if x %}{{ x }}{%- else -%}foo{%- endif %}␊
                -id="xxx"␊
                aria-x="foo">␊
                Hello World␊
              </li>␊
            </ul>␊
          </div>␊
        </li>␊
      </ul>␊
    </div>`

> ### Snapshot 5
> Testing nested attribute forcing. This case is rather common and intends to affirm the correct output structures
> typical in most projects but also an exteme edge case scenario.

    `<div id="foo" data-bar class="xxxxx" {{ output.attr | filter: '' }}>␊
      <ul>␊
        <li {% if x %}{{ attr.2 }}{% endif %}>Hello</li>␊
        <li data-attr-1="one" data-attr-2="two" data-attr-3="three">␊
          <a href="/some-url"␊
            {% unless xx %} data-boolean {% else %} id="some-id"{% endunless %}>␊
            Click me!␊
          </a>␊
        </li>␊
        <li data-attr-one="1"␊
          data-attr-two="2"␊
          data-attr-three="3"␊
          {% for x in arr %}␊
          data-{{ x }}␊
          {% endfor %}>␊
          <div id="{{ id | filter: 'foo' }}"␊
            data-{% if x %}id="foo"{% else %}name{% endif %}>␊
            <ul class="xxxxxx" aria-attr="xxxxxxxx" {{ output.attr | filter: '' }}>␊
              <li id="x"␊
                class="xxx xxx xxx"␊
                data-{% if x %}{{ x }}{%- else -%}foo{%- endif %}␊
                -id="xxx"␊
                aria-x="foo">␊
                Hello World␊
              </li>␊
            </ul>␊
          </div>␊
        </li>␊
      </ul>␊
    </div>`

> ### Snapshot 5
> Testing nested attribute forcing. This case is rather common and intends to affirm the correct output structures
> typical in most projects but also an exteme edge case scenario.

    `<div id="foo" data-bar class="xxxxx" {{ output.attr | filter: '' }}>␊
      <ul>␊
        <li {% if x %}{{ attr.2 }}{% endif %}>Hello</li>␊
        <li data-attr-1="one" data-attr-2="two" data-attr-3="three">␊
          <a href="/some-url"␊
            {% unless xx %} data-boolean {% else %} id="some-id"{% endunless %}>␊
            Click me!␊
          </a>␊
        </li>␊
        <li data-attr-one="1" data-attr-two="2" data-attr-three="3" {% for x in arr %} data-{{ x }} {% endfor %}>␊
          <div id="{{ id | filter: 'foo' }}"␊
            data-{% if x %}id="foo"{% else %}name{% endif %}>␊
            <ul class="xxxxxx" aria-attr="xxxxxxxx" {{ output.attr | filter: '' }}>␊
              <li id="x" class="xxx xxx xxx" data-{% if x %}{{ x }}{%- else -%}foo{%- endif %} -id="xxx" aria-x="foo">␊
                Hello World␊
              </li>␊
            </ul>␊
          </div>␊
        </li>␊
      </ul>␊
    </div>`

> ### Snapshot 5
> Testing nested attribute forcing. This case is rather common and intends to affirm the correct output structures
> typical in most projects but also an exteme edge case scenario.

    `<div id="foo" data-bar class="xxxxx" {{ output.attr | filter: '' }}>␊
      <ul>␊
        <li {% if x %}{{ attr.2 }}{% endif %}>Hello</li>␊
        <li data-attr-1="one" data-attr-2="two" data-attr-3="three">␊
          <a href="/some-url"␊
            {% unless xx %} data-boolean {% else %} id="some-id"{% endunless %}>␊
            Click me!␊
          </a>␊
        </li>␊
        <li data-attr-one="1" data-attr-two="2" data-attr-three="3" {% for x in arr %} data-{{ x }} {% endfor %}>␊
          <div id="{{ id | filter: 'foo' }}"␊
            data-{% if x %}id="foo"{% else %}name{% endif %}>␊
            <ul class="xxxxxx" aria-attr="xxxxxxxx" {{ output.attr | filter: '' }}>␊
              <li id="x" class="xxx xxx xxx" data-{% if x %}{{ x }}{%- else -%}foo{%- endif %} -id="xxx" aria-x="foo">␊
                Hello World␊
              </li>␊
            </ul>␊
          </div>␊
        </li>␊
      </ul>␊
    </div>`
