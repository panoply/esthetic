{% # Assertive, clear and very alpha 🐐 %}

{% render 'file'
  , param_1: 'a'
  , param_2: 'a'
  , param_3: 'a'
  , param_4: 'a'
%}

{{ object.prop | filter: 200 | filter: linebreak: 'a', param_2: 'b', param_3: 'c', param_4: 'd'
}}

{{ object.property[foo][0][100].prop[object.prop[0]] }}
{% assign x = object.xxx['prop'][0][[item[0]][item[1]]].prop.foo[0][1] %}

{% if foo <= bar
  or foo <=  condition_expression
  or another_conditional_type <= bar
  or foo <= bar
%}

  {%
    if foo <= bar or foo <= bar or foo <= bar
  %}

    {{
      settings.logo | image_url: width: 600 | image_tag: class: 'header__heading-logo', widths: widths, height: logo_height , width: settings.logo_width ,  alt: logo_alt, sizes: sizes, preload: true }}

    {%
      render 'snippet'
      , param_1: true
      , param_2: 1000
      , param_3: 'string'
      , param_4: nil %}

  {% endif %}

{% endif %}