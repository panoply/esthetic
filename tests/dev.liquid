<style>

  {{ settings.type_body_font | font_face: font_display: 'swap' }}

  {% for scheme in settings.color_schemes %}

    {% assign scheme_classes = scheme_classes | append: ', .color-' | append: scheme.id %} {% if forloop.index == 1 %}
    : root,
  {% endif %}

  .color-{{ scheme.id }} {
  
      }

{% endstyle %}